import Project from './projects';
import Nav from './nav';

document.addEventListener("DOMContentLoaded", () => {
  const defaultFilter = document.querySelector('.portfolio-filters');
  defaultFilter.classList.add('mixitup-control-active');

});
