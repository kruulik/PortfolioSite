
class Project {
  constructor(options) {
    this.project = "";
    // options maybe, but not sure how to pass that down from HTML
    this.projectItems = [];
  }

  // need function to capture project name, and then set const with array of assets

  

}
